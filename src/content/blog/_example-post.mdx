---
title: "Example post (delete me)"
description: "Template showing the frontmatter fields the blog schema expects."
pubDate: 2026-07-17
tags: ["meta"]
draft: true
repo: "https://github.com/ggasg/example-repo"
---

This file is a template, not a real post. It's excluded from the build
(`draft: true`) and from the sitemap/RSS feed. Delete it once you've copied
the frontmatter shape into your first real post.

Available frontmatter fields: `title`, `description`, `pubDate`,
`updatedDate` (optional), `draft`, `tags`, `repo` (optional link to the
companion code repo), `heroImage` (optional).

Code blocks get syntax highlighting out of the box:

```rust
fn main() {
    println!("this renders with the github-dark theme");
}
```
