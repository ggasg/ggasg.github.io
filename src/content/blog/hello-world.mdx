---
title: "Hello, world (delete me)"
description: "Scaffold post used to verify the build pipeline. Not meant to be published."
pubDate: 2026-07-17
tags: ["meta"]
draft: true
repo: "https://github.com/ggasg/example-repo"
---

This is a real entry in the `blog` collection (unlike `_example-post.mdx`,
which is excluded from the collection entirely because of its leading
underscore, that one is just a frontmatter reference). This one exists to
confirm the schema, routing, RSS, and sitemap all work end to end.

It's marked `draft: true`, so it won't appear in the production build, the
sitemap, or the RSS feed. It only shows up in `astro dev`. Delete it once
your first real post is ready.

```rust
fn main() {
    println!("code blocks get syntax highlighting out of the box");
}
```
